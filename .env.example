# 로컬 개발용 SQLite (Turso 변수가 없으면 이걸 사용)
DATABASE_URL="file:./dev.db"

# 배포(Vercel) 시 Turso 원격 DB — 둘 다 설정하면 로컬에서도 Turso를 사용
TURSO_DATABASE_URL=""
TURSO_AUTH_TOKEN=""

# 사이트 전체 접근을 막는 공용 비밀번호 (Basic Auth). 비워두면 인증 없이 접근 가능.
SITE_PASSWORD=""
